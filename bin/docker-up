#!/usr/bin/env bash
# Script para subir a aplicação via Docker

set -e

echo "🚀 Iniciando a aplicação Survival Backpack via Docker..."

# Verificar se docker está instalado
if ! command -v docker &> /dev/null; then
    echo "❌ Docker não está instalado. Por favor, instale o Docker primeiro."
    exit 1
fi

# Verificar se docker compose está disponível
if ! docker compose version &> /dev/null; then
    echo "❌ Docker Compose não está disponível. Por favor, atualize o Docker para uma versão que suporte 'docker compose'."
    exit 1
fi

# Subir os containers
docker compose up -d

echo "✅ Aplicação iniciada com sucesso!"
echo "📍 Acesse a aplicação em: http://localhost:3000"
echo ""
echo "Comandos úteis:"
echo "  - Ver logs: bin/docker-logs"
echo "  - Executar testes: bin/docker-test"
echo "  - Console: bin/docker-console"
echo "  - Parar app: bin/docker-stop"

