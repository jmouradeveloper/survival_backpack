<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <title>Survival Backpack - Gerenciamento de Estoque</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-cache-control" content="no-cache">
    <meta charset="UTF-8">
    <meta name="vapid-public-key" content="BGPQxnL5eWKvTY7_aa1CsEsxdulj9c3MrQowgzkwBiNFHch-KQ9VGzZy9J_2C7aau3nc7vYjdprAM8ymzzfzaAU">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- PWA Configuration -->
    <link rel="manifest" href="<%= pwa_manifest_path %>">
    <meta name="theme-color" content="#2563eb">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Survival Backpack">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= yield :head %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <!-- Flatpickr CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    
    <%= javascript_importmap_tags %>
    
    <!-- Flatpickr JS -->
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/pt.js"></script>
    
    <!-- Service Worker Registration -->
    <script>
      if ('serviceWorker' in navigator) {
        navigator.serviceWorker.register('/service-worker').then(registration => {
          console.log('Service Worker registrado com sucesso:', registration);
        }).catch(error => {
          console.log('Falha ao registrar Service Worker:', error);
        });
      }
    </script>
  </head>

  <body data-controller="notifications pwa-guide">
    <%= render "shared/pwa_guide_modal" %>
    
    <header class="main-header">
      <div class="container">
        <h1>🎒 Survival Backpack</h1>
        <nav>
          <%= link_to "Alimentos", food_items_path, class: "nav-link" %>
          <%= link_to "📦 Lotes FIFO", supply_batches_path, class: "nav-link" %>
          <%= link_to "🔄 Rotações", supply_rotations_path, class: "nav-link" %>
          <%= link_to notifications_path, class: "nav-link notifications-link" do %>
            <span>🔔 Notificações</span>
            <span class="notification-badge hidden" data-notifications-target="badge">0</span>
          <% end %>
          <%= link_to "⚙️ Configurações", notification_preferences_path, class: "nav-link" %>
        </nav>
      </div>
    </header>

    <main class="container">
      <% if flash[:notice] %>
        <div class="alert alert-success" data-controller="auto-dismiss">
          <%= flash[:notice] %>
        </div>
      <% end %>

      <% if flash[:alert] %>
        <div class="alert alert-error" data-controller="auto-dismiss">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="main-footer">
      <div class="container">
        <p>&copy; 2025 Survival Backpack - Sistema de Gerenciamento de Estoque</p>
      </div>
    </footer>
  </body>
</html>
