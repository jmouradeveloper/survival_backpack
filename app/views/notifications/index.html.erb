<div class="notifications-page" data-controller="notifications">
  <!-- Modal Proativo de Permissão de Notificações -->
  <div class="modal-backdrop" 
       data-notifications-target="modalBackdrop"
       data-action="click->notifications#closeModalOnBackdrop">
  </div>
  
  <div class="permission-modal" data-notifications-target="permissionModal">
    <div class="permission-modal-content">
      <div class="permission-modal-header modal-header">
        <div class="permission-modal-icon">🔔</div>
        <h3 class="modal-title">Ative as Notificações</h3>
      </div>
      
      <div class="permission-modal-body modal-body">
        <p class="permission-modal-description">
          Nunca mais perca alimentos! Receba alertas inteligentes sobre a validade dos seus itens.
        </p>
        
        <ul class="benefits-list permission-modal-benefits">
          <li class="benefit-item">
            <span class="benefit-icon">✅</span>
            <span class="benefit-text">Alertas antes do vencimento</span>
          </li>
          <li class="benefit-item">
            <span class="benefit-icon">📱</span>
            <span class="benefit-text">Funciona mesmo offline</span>
          </li>
          <li class="benefit-item">
            <span class="benefit-icon">🔒</span>
            <span class="benefit-text">Seus dados são privados</span>
          </li>
          <li class="benefit-item">
            <span class="benefit-icon">🌐</span>
            <span class="benefit-text">Acesso em qualquer lugar</span>
          </li>
        </ul>
        
        <p class="permission-modal-note">
          <small>💡 Você pode desativar as notificações a qualquer momento nas configurações.</small>
        </p>
      </div>
      
      <div class="permission-modal-footer modal-footer">
        <%= button_tag "🚀 Ativar Agora", 
            class: "btn btn-primary btn-lg",
            data: { action: "notifications#requestPermissionFromModal" } %>
        <%= button_tag "⏭️ Mais Tarde", 
            class: "btn btn-secondary-outline",
            data: { action: "notifications#closePermissionModal" } %>
      </div>
    </div>
  </div>

  <div class="page-header">
    <h2>📬 Notificações</h2>
    
    <div class="notification-actions">
      <% if @unread_count > 0 %>
        <%= button_to "Marcar todas como lidas", 
            mark_all_as_read_notifications_path, 
            method: :post,
            class: "btn btn-secondary",
            data: { action: "notifications#markAllAsRead" } %>
      <% end %>
    </div>
  </div>

  <!-- Solicitação de permissão de notificações -->
  <div class="notification-permission hidden" data-notifications-target="permission">
    <div class="alert alert-info">
      <p>
        <strong>🔔 Ative as notificações</strong>
        <br>
        Receba alertas sobre a validade dos seus alimentos, mesmo quando o app estiver fechado.
      </p>
      <%= button_tag "Ativar Notificações", 
          class: "btn btn-primary",
          data: { action: "notifications#requestPermission" } %>
    </div>
  </div>

  <!-- Contador de não lidas -->
  <% if @unread_count > 0 %>
    <div class="notifications-summary">
      <p class="unread-count">
        Você tem <strong><%= @unread_count %></strong> notificação(ões) não lida(s)
      </p>
    </div>
  <% end %>

  <!-- Lista de notificações -->
  <%= turbo_frame_tag "notifications_list", data: { notifications_target: "list" } do %>
    <div class="notifications-list">
      <% if @notifications.any? %>
        <%= render @notifications %>
      <% else %>
        <div class="empty-state">
          <p>📭 Nenhuma notificação ainda</p>
          <p class="text-muted">Você será notificado quando seus alimentos estiverem próximos da validade.</p>
        </div>
      <% end %>
    </div>
  <% end %>
</div>

<style>
.notifications-page {
  padding: 1.5rem;
}

.page-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 2rem;
}

.notification-permission {
  margin-bottom: 1.5rem;
}

.notification-permission.hidden {
  display: none;
}

.alert {
  padding: 1rem;
  border-radius: 8px;
  margin-bottom: 1rem;
}

.alert-info {
  background-color: #e3f2fd;
  border: 1px solid #2196f3;
  color: #1565c0;
}

.notifications-summary {
  background-color: #fff3e0;
  border: 1px solid #ff9800;
  border-radius: 8px;
  padding: 1rem;
  margin-bottom: 1.5rem;
}

.notifications-summary .unread-count {
  margin: 0;
  color: #e65100;
}

.notifications-list {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.empty-state {
  text-align: center;
  padding: 3rem 1rem;
  color: #666;
}

.empty-state p:first-child {
  font-size: 2rem;
  margin-bottom: 0.5rem;
}

.text-muted {
  color: #999;
  font-size: 0.9rem;
}

/* ==================== MODAL PROATIVO DE PERMISSÃO ==================== */

.modal-backdrop {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.6);
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.3s ease, visibility 0.3s ease;
  backdrop-filter: blur(4px);
}

.modal-backdrop.show {
  opacity: 1;
  visibility: visible;
}

.permission-modal {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.9);
  z-index: 1001;
  max-width: 500px;
  width: 90%;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.permission-modal.show {
  opacity: 1;
  visibility: visible;
  transform: translate(-50%, -50%) scale(1);
}

.permission-modal-content {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 20px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  overflow: hidden;
  animation: slideInUp 0.4s ease-out;
}

@keyframes slideInUp {
  from {
    transform: translateY(20px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.permission-modal-header {
  background: rgba(255, 255, 255, 0.1);
  padding: 2rem;
  text-align: center;
  backdrop-filter: blur(10px);
}

.permission-modal-icon {
  font-size: 4rem;
  margin-bottom: 1rem;
  animation: bounce 1s ease infinite;
}

@keyframes bounce {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10px);
  }
}

.permission-modal-header h3 {
  color: white;
  font-size: 1.8rem;
  font-weight: 700;
  margin: 0;
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
}

.permission-modal-body {
  padding: 2rem;
  background: white;
}

.permission-modal-description {
  font-size: 1.1rem;
  color: #333;
  text-align: center;
  margin-bottom: 1.5rem;
  line-height: 1.6;
}

.benefits-list,
.permission-modal-benefits {
  list-style: none;
  padding: 0;
  margin: 0 0 1.5rem 0;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.benefit-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 0.75rem 1rem;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  border-radius: 12px;
  transition: transform 0.2s ease;
}

.benefit-item:hover {
  transform: translateX(5px);
}

.benefit-icon {
  font-size: 1.5rem;
  min-width: 30px;
  text-align: center;
}

.benefit-text {
  font-size: 1rem;
  color: #333;
  font-weight: 500;
}

.permission-modal-note {
  text-align: center;
  color: #666;
  margin: 0;
  padding: 1rem;
  background: #f8f9fa;
  border-radius: 8px;
}

.permission-modal-note small {
  font-size: 0.9rem;
  line-height: 1.4;
}

.permission-modal-footer {
  padding: 1.5rem 2rem 2rem;
  background: white;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.btn {
  padding: 0.75rem 1.5rem;
  border: none;
  border-radius: 12px;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  text-align: center;
  display: inline-block;
}

.btn-primary {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
}

.btn-primary:active {
  transform: translateY(0);
}

.btn-lg {
  padding: 1rem 2rem;
  font-size: 1.1rem;
}

.btn-secondary-outline {
  background: transparent;
  color: #666;
  border: 2px solid #ddd;
  padding: 0.6rem 1.5rem;
}

.btn-secondary-outline:hover {
  background: #f5f5f5;
  border-color: #999;
  color: #333;
}

/* Responsividade para telas menores */
@media (max-width: 600px) {
  .permission-modal {
    width: 95%;
    max-width: none;
  }
  
  .permission-modal-header {
    padding: 1.5rem;
  }
  
  .permission-modal-icon {
    font-size: 3rem;
  }
  
  .permission-modal-header h3 {
    font-size: 1.5rem;
  }
  
  .permission-modal-body {
    padding: 1.5rem;
  }
  
  .permission-modal-description {
    font-size: 1rem;
  }
  
  .benefit-text {
    font-size: 0.9rem;
  }
  
  .permission-modal-footer {
    padding: 1rem 1.5rem 1.5rem;
  }
}

/* Dark mode support (opcional) */
@media (prefers-color-scheme: dark) {
  .permission-modal-body {
    background: #2d2d2d;
  }
  
  .permission-modal-description {
    color: #e0e0e0;
  }
  
  .benefit-item {
    background: linear-gradient(135deg, #3a3a3a 0%, #2d2d2d 100%);
  }
  
  .benefit-text {
    color: #e0e0e0;
  }
  
  .permission-modal-note {
    background: #3a3a3a;
    color: #b0b0b0;
  }
  
  .btn-secondary-outline {
    color: #b0b0b0;
    border-color: #555;
  }
  
  .btn-secondary-outline:hover {
    background: #3a3a3a;
    border-color: #777;
    color: #e0e0e0;
  }
}
</style>

