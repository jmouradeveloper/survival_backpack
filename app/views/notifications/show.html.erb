<div class="notification-detail" data-controller="notifications">
  <div class="page-header">
    <h2>📬 Detalhes da Notificação</h2>
    <%= link_to "← Voltar", notifications_path, class: "btn btn-secondary" %>
  </div>

  <div class="notification-card <%= @notification.read? ? 'read' : 'unread' %>">
    <div class="notification-header">
      <h3><%= @notification.title %></h3>
      <span class="priority-badge priority-<%= @notification.priority %>">
        <%= case @notification.priority
            when 2 then "🔴 Alta"
            when 1 then "🟡 Média"  
            else "🔵 Baixa"
            end %>
      </span>
    </div>

    <div class="notification-body">
      <p><%= @notification.body %></p>
    </div>

    <% if @notification.food_item %>
      <div class="related-food-item">
        <h4>📦 Item Relacionado:</h4>
        <div class="food-item-info">
          <p><strong>Nome:</strong> <%= @notification.food_item.name %></p>
          <p><strong>Categoria:</strong> <%= @notification.food_item.category %></p>
          <p><strong>Validade:</strong> <%= l(@notification.food_item.expiration_date, format: :long) %></p>
          <p><strong>Localização:</strong> <%= @notification.food_item.storage_location %></p>
        </div>
        <%= link_to "Ver detalhes do item", food_item_path(@notification.food_item), class: "btn btn-primary" %>
      </div>
    <% end %>

    <div class="notification-meta">
      <p><strong>Tipo:</strong> <%= @notification.notification_type.humanize %></p>
      <p><strong>Enviada em:</strong> <%= l(@notification.sent_at || @notification.created_at, format: :long) %></p>
      <p><strong>Status:</strong> <%= @notification.read? ? "✅ Lida" : "⚪ Não lida" %></p>
    </div>

    <div class="notification-actions">
      <% unless @notification.read? %>
        <%= button_to "Marcar como lida", 
            mark_as_read_notification_path(@notification), 
            method: :post,
            class: "btn btn-primary" %>
      <% end %>
      <%= button_to "Excluir notificação", 
          notification_path(@notification), 
          method: :delete,
          data: { confirm: "Tem certeza que deseja excluir esta notificação?" },
          class: "btn btn-danger" %>
    </div>
  </div>
</div>

<style>
.notification-detail {
  padding: 1.5rem;
  max-width: 800px;
  margin: 0 auto;
}

.notification-card {
  background: white;
  border-radius: 12px;
  padding: 2rem;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  margin-top: 1.5rem;
}

.notification-card.unread {
  border-left: 4px solid #2196f3;
}

.notification-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 1.5rem;
  padding-bottom: 1rem;
  border-bottom: 2px solid #f0f0f0;
}

.notification-header h3 {
  margin: 0;
  color: #333;
  flex: 1;
}

.priority-badge {
  padding: 0.5rem 1rem;
  border-radius: 20px;
  font-size: 0.9rem;
  font-weight: 600;
  white-space: nowrap;
}

.priority-badge.priority-2 {
  background-color: #ffebee;
  color: #c62828;
}

.priority-badge.priority-1 {
  background-color: #fff3e0;
  color: #e65100;
}

.priority-badge.priority-0 {
  background-color: #e3f2fd;
  color: #1565c0;
}

.notification-body {
  margin-bottom: 2rem;
  line-height: 1.6;
}

.notification-body p {
  color: #555;
  font-size: 1.1rem;
}

.related-food-item {
  background-color: #f8f9fa;
  border-radius: 8px;
  padding: 1.5rem;
  margin-bottom: 2rem;
}

.related-food-item h4 {
  margin-top: 0;
  color: #333;
}

.food-item-info {
  margin: 1rem 0;
}

.food-item-info p {
  margin: 0.5rem 0;
  color: #555;
}

.notification-meta {
  background-color: #fafafa;
  border-radius: 8px;
  padding: 1rem;
  margin-bottom: 1.5rem;
}

.notification-meta p {
  margin: 0.5rem 0;
  color: #666;
}

.notification-actions {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.btn {
  padding: 0.75rem 1.5rem;
  border: none;
  border-radius: 8px;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  text-decoration: none;
  display: inline-block;
  text-align: center;
  transition: all 0.3s ease;
}

.btn-primary {
  background-color: #2196f3;
  color: white;
}

.btn-primary:hover {
  background-color: #1976d2;
  transform: translateY(-2px);
}

.btn-secondary {
  background-color: #757575;
  color: white;
}

.btn-secondary:hover {
  background-color: #616161;
}

.btn-danger {
  background-color: #f44336;
  color: white;
}

.btn-danger:hover {
  background-color: #d32f2f;
}

@media (max-width: 600px) {
  .notification-header {
    flex-direction: column;
    gap: 1rem;
  }

  .priority-badge {
    align-self: flex-start;
  }

  .notification-actions {
    flex-direction: column;
  }

  .btn {
    width: 100%;
  }
}
</style>

